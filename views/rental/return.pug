extends ../layout 

block main
  section(class="section")
    div(class="columns is-mobile")
      div(class="column")
        div(class="container")
          div(class="content")
            form(action="/rental/"+returnDB._id+"/return" method="post")
              section(class="section")
                h1= returnDB.name + '을(를) 반납 하시겠습니까?'
                h2 반납 확인
                p= returnUser
              section(class="section")
                div(class="field is-grouped")
                  p(class="control")
                    input(class="button is-success" type="submit" value="확인")
                  p(class="control")
                    a(class="button" href="/rental") 취소
