extends ../layout

block main
  section(class="hero is-fullheight")
    div(class="hero-body")
      div(class="container has-text-centered")
        div(class="content")
          h1 접근 권한이 없습니다.
          a(class="button is-large" onclick="goBack()")
            span 돌아가기
  script(src="/source/javascript/go-back-1.0.js") 
